# Porta do servidor
PORT=3000

# MongoDB - Local
# MONGO_URI=mongodb://localhost:27017/backend-em-express

# MongoDB - Atlas (Produção)
# Substitua <username>, <password> e <cluster> pelos seus dados
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/backend-em-express?retryWrites=true&w=majority

# JWT - Chave secreta (use um valor aleatório e seguro em produção)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_change_in_production

# JWT - Tempo de expiração do token (exemplos: 1h, 7d, 30d)
JWT_EXPIRES_IN=1d
